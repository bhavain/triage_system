{
  "name": "triage-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build && mkdir -p public && cp -r ../frontend/dist/* public/",
    "start": "cd backend && npm run start:prod"
  },
  "engines": {
    "node": ">=20.x"
  }
}
